// tab-panel 1.0.0 6/1/2021 11:13:01 PM
let e;
class t extends HTMLElement {
    loadSlot() {
        const t = this.querySelector('[slot="panel"]');
        t && (e = t.outerHTML)
    }
    renderTabPanel() {
        const t = this.getAttribute("tab_panel_id");
        return `<div class="tabcontent" data-id="${t}" aria-labelledby="${t}">\n                  ${e}\n                </div>`
    }
    render() {
        this.classList.add("c-tab__panel"), this.innerHTML = "" + this.renderTabPanel()
    }
    connectedCallback() {
        this.loadSlot(), this.render()
    }
}
customElements.get("n-tab-panel") || customElements.define("n-tab-panel", t);
export default t;